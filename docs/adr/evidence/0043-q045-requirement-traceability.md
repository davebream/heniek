# Q045 requirement traceability

| Requirement | Implementation | Deterministic evidence |
|---|---|---|
| Persist before side effects | Finding and causal replan are inserted before cancellation is requested | State store transition tests and coordinator race test |
| Quiesce affected work safely | Only affected attempts receive cancellation; leases remain until acknowledgement | Coordinator cancellation and lease assertions |
| Preserve safe siblings | Unaffected tasks continue through ordinary scheduler and integration gates | Hidden-dependency race with T2, T3, and T5 retained |
| Evidence-linked supersession | V2 trigger names the finding; supersede evidence and affected task set are validated | Pipeline authorization, missing-evidence, and stale-proposal tests |
| Preserve V1 freeze behavior | Started-task mutations remain invalid under V1 proposals | V1 regression assertion in revision tests |
| Revalidate the replacement DAG | Existing pipeline validation covers requirements, cycles, write sets, waves, and live capacities | Requirement-loss, topology, write-conflict, and capacity test cases |
| Restart-safe causal lifecycle | Replan transitions are durable and an already-committed V2 revision is adopted on replay | State crash/restart tests and coordinator revision-commit replay path |
| Active-DAG planning with historical audit | Scheduler filters planning inputs to active task IDs while prior task records remain stored | Wave-store filter and coordinator history assertions |
| Canonical five-task acceptance | T1 integrates first; T2–T5 share one valid wave across exactly three repositories | Five-task daemon acceptance test and run export |
| Truthful execution paths | Retry and question/resume reuse child sessions; integration is serialized; combined verification is required | Acceptance driver trace and child identity assertions |
| Typed non-continuation | Missing child, failed prerequisite, reconciliation, missing evidence, rejected revision, and incomplete cancellation block | Coordinator and state blocker tests |
| Public compatibility | Additive V1 finding/replan and V2 revision schemas; prior V1 generated bytes unchanged | Contract digest assertions and generated-artifact check |
