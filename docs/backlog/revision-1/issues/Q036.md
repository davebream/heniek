<!-- heniek-queue {"revision":1,"sequence":"Q036","previous":"Q035","milestone":"M4","queueHash":"b29fc62f94d7810be5f065d73934504848dab9000997f141351242bc104af908"} -->

# Q036 — Implement isolated variants, one-writer leases and expected-SHA integration

## Outcome

Parallel work uses isolated composite variants with per-checkout writer leases, and integration updates refs only when expected SHAs still match.

## Specification anchors

- §12.7 Writer isolation
- §12.8 Variant integration
- §21 Git branch and delivery model

## Dependency

Requires Q035 to be merged and independently confirmed on remote `main`.

## Tier

`T2-capability`

## Constraints

- One live writer per concrete checkout.
- Integration is repository-scoped and replay-safe.
- Preserve Product Specification v0.2 commitments and acceptance criteria.
- Keep Windmill, predecessor automation tooling, and TAKT out of Heniek runtime dependencies.
- Use strict ESM TypeScript, provider-neutral public contracts, and repository conventions.

## Exclusions

- No task-wave scheduler.
- No force push over unexpected external changes.
- No factory runtime state, credentials, transcripts, or control artifacts in this repository.
- No unrelated backlog work or speculative scope reduction.

## Acceptance criteria

- [ ] Variants clone or worktree every declared write target and share read-only context safely.
- [ ] Lease conflicts block before mutation and recover after verified owner death.
- [ ] Integration applies prepared commits with expected-SHA guards and classifies conflicts/partial progress.
- [ ] Public contracts and generated schemas remain compatible or are versioned deliberately.
- [ ] `pnpm check` passes from a clean checkout on Node.js 24.
- [ ] The pull request is non-draft, self-contained, and uses `Closes <this issue>`.

## Required tests

- Parallel writer and lease recovery stress tests.
- Expected-SHA race, conflict, retry, and idempotency tests.
- Regression tests for every defect discovered while implementing this issue.

## Required evidence

- Variant/lease inventory.
- Integration transaction trace.
- Exact commands and results for local checks.
- GitHub required-check and merge confirmation.

## Autonomous stop conditions

Stop with a typed blocker and evidence if the predecessor is not merged, queue
markers disagree, a product commitment would need to be narrowed, a required
subscription route cannot be proven, credentials could be exposed, or a genuine
technical ambiguity remains after the issue's bounded spike/ADR path.
