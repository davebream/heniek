<!-- heniek-queue {"revision":1,"sequence":"Q004","previous":"Q003","milestone":"M0","queueHash":"b29fc62f94d7810be5f065d73934504848dab9000997f141351242bc104af908"} -->

# Q004 — Spike B: isolated subscription-only Claude and Codex profiles under hostile ambient env

## Outcome

A reproducible isolation recipe proves Claude Code and Codex use named subscription credentials even when the parent environment contains conflicting API keys and credentials.

## Specification anchors

- §9.1 Accounts
- §10.4 Billing guard
- §27.4 Secrets and logs

## Dependency

Requires Q003 to be merged and independently confirmed on remote `main`.

## Tier

`T0-evidence`

## Constraints

- Use dedicated temporary config homes and an allowlisted environment.
- Prove billing route from provider-visible identity or CLI diagnostics, not process exit alone.
- Preserve Product Specification v0.2 commitments and acceptance criteria.
- Keep Windmill, predecessor automation tooling, and TAKT out of Heniek runtime dependencies.
- Use strict ESM TypeScript, provider-neutral public contracts, and repository conventions.

## Exclusions

- No metered API request.
- No copying the user's general-purpose credential stores into test fixtures.
- No factory runtime state, credentials, transcripts, or control artifacts in this repository.
- No unrelated backlog work or speculative scope reduction.

## Acceptance criteria

- [ ] Claude runs with Max/subscription auth and no ANTHROPIC_API_KEY exposure.
- [ ] Codex runs with saved ChatGPT auth and no OPENAI_API_KEY or CODEX_API_KEY exposure.
- [ ] Hostile ambient keys, config precedence, expiry, and logout paths have regression coverage.
- [ ] Public contracts and generated schemas remain compatible or are versioned deliberately.
- [ ] `pnpm check` passes from a clean checkout on Node.js 24.
- [ ] The pull request is non-draft, self-contained, and uses `Closes <this issue>`.

## Required tests

- Hostile-environment matrix with sentinel credentials.
- Credential expiry and revocation canaries.
- Regression tests for every defect discovered while implementing this issue.

## Required evidence

- Redacted environment diff.
- Billing-route attestation and canary transcripts.
- Exact commands and results for local checks.
- GitHub required-check and merge confirmation.

## Autonomous stop conditions

Stop with a typed blocker and evidence if the predecessor is not merged, queue
markers disagree, a product commitment would need to be narrowed, a required
subscription route cannot be proven, credentials could be exposed, or a genuine
technical ambiguity remains after the issue's bounded spike/ADR path.
