<!-- heniek-queue {"revision":1,"sequence":"Q027","previous":"Q026","milestone":"M3","queueHash":"61fee7f076d3946cfa2a4b82f73f3ec72cf9e8eba6c31cc891277dc7ddd7525a"} -->

# Q027 — Implement approval, integration, verify and publish stage runners

## Outcome

The remaining fixed stage runners provide durable human gates, expected-SHA integration, independent verification, and ForgeBackend publication.

## Specification anchors

- §14.2 First-class stage types
- §19 Review and verification
- §21 Git branch and delivery model

## Dependency

Requires Q026 to be merged and independently confirmed on remote `main`.

## Tier

`T2-capability`

## Constraints

- Approval waits durably; it never auto-answers in HITL mode.
- Integration and publish operations are idempotent and reconcile external state.
- Preserve Product Specification v0.2 commitments and acceptance criteria.
- Keep Windmill, Kombajn, and TAKT out of Heniek runtime dependencies.
- Use strict ESM TypeScript, provider-neutral public contracts, and repository conventions.

## Exclusions

- No GitHub-specific logic outside adapters.
- No automatic post-v1 CI watcher.
- No factory runtime state, credentials, transcripts, or control artifacts in this repository.
- No unrelated backlog work or speculative scope reduction.

## Acceptance criteria

- [ ] Approval records prompt, decision, actor, revision, and continuation.
- [ ] Integration verifies source/base SHAs and classifies conflicts without partial success claims.
- [ ] Verify and publish require explicit contracts and recover adopted external resources after restart.
- [ ] Public contracts and generated schemas remain compatible or are versioned deliberately.
- [ ] `pnpm check` passes from a clean checkout on Node.js 24.
- [ ] The pull request is non-draft, self-contained, and uses `Closes <this issue>`.

## Required tests

- Per-runner lifecycle and idempotency suites.
- Stale SHA, conflict, rejected approval, duplicate publish, and restart tests.
- Regression tests for every defect discovered while implementing this issue.

## Required evidence

- Runner conformance matrix.
- Reconciliation traces for integration and publication.
- Exact commands and results for local checks.
- GitHub required-check and merge confirmation.

## Autonomous stop conditions

Stop with a typed blocker and evidence if the predecessor is not merged, queue
markers disagree, a product commitment would need to be narrowed, a required
subscription route cannot be proven, credentials could be exposed, or a genuine
technical ambiguity remains after the issue's bounded spike/ADR path.
