<!-- heniek-queue {"revision":1,"sequence":"Q025","previous":"Q024","milestone":"M3","queueHash":"b29fc62f94d7810be5f065d73934504848dab9000997f141351242bc104af908"} -->

# Q025 — Implement deterministic graph scheduling and the fixed stage state machine

## Outcome

A pure deterministic scheduler computes eligible stages and transitions for agent, command, approval, integration, verify, and publish nodes.

## Specification anchors

- §14 Pipeline model
- §16 Canonical run state and artifacts
- §20.2 Task-level DAG

## Dependency

Requires Q024 to be merged and independently confirmed on remote `main`.

## Tier

`T1-foundation`

## Constraints

- Scheduling decisions depend only on canonical state and time inputs.
- Persist decisions before dispatching side effects.
- Preserve Product Specification v0.2 commitments and acceptance criteria.
- Keep Windmill, predecessor automation tooling, and TAKT out of Heniek runtime dependencies.
- Use strict ESM TypeScript, provider-neutral public contracts, and repository conventions.

## Exclusions

- No stage-specific runner implementation.
- No model-driven control-flow mutation.
- No factory runtime state, credentials, transcripts, or control artifacts in this repository.
- No unrelated backlog work or speculative scope reduction.

## Acceptance criteria

- [ ] State machine covers pending, ready, queued, running, waiting, retrying, succeeded, failed, cancelled, and blocked outcomes.
- [ ] Parallel eligibility, fan-in, cancellation, retry, and terminal-run computation are deterministic.
- [ ] Duplicate ticks and restart replay cannot dispatch the same attempt twice.
- [ ] Public contracts and generated schemas remain compatible or are versioned deliberately.
- [ ] `pnpm check` passes from a clean checkout on Node.js 24.
- [ ] The pull request is non-draft, self-contained, and uses `Closes <this issue>`.

## Required tests

- State-transition and graph property tests.
- Randomized replay/idempotency tests with fake time.
- Regression tests for every defect discovered while implementing this issue.

## Required evidence

- State-machine table and generated diagram data.
- Seeded scheduler replay log.
- Exact commands and results for local checks.
- GitHub required-check and merge confirmation.

## Autonomous stop conditions

Stop with a typed blocker and evidence if the predecessor is not merged, queue
markers disagree, a product commitment would need to be narrowed, a required
subscription route cannot be proven, credentials could be exposed, or a genuine
technical ambiguity remains after the issue's bounded spike/ADR path.
