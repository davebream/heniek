<!-- heniek-queue {"revision":1,"sequence":"Q051","previous":"Q050","milestone":"M7","queueHash":"61fee7f076d3946cfa2a4b82f73f3ec72cf9e8eba6c31cc891277dc7ddd7525a"} -->

# Q051 — Complete deterministic CLI operations

## Outcome

The `heniek` CLI covers setup, Codebases, profiles, pipelines, runs, interactions, recovery, diagnostics, logs, backup, and export with stable JSON output.

## Specification anchors

- §6.2 Client surfaces
- §25 Notifications and inbox
- §26 Retention, backup, and export

## Dependency

Requires Q050 to be merged and independently confirmed on remote `main`.

## Tier

`T2-capability`

## Constraints

- CLI is a thin JSON-RPC client.
- Every mutating command supports clear confirmation/idempotency semantics.
- Preserve Product Specification v0.2 commitments and acceptance criteria.
- Keep Windmill, Kombajn, and TAKT out of Heniek runtime dependencies.
- Use strict ESM TypeScript, provider-neutral public contracts, and repository conventions.

## Exclusions

- No unique orchestration logic.
- No output that depends on terminal color or prose for automation.
- No factory runtime state, credentials, transcripts, or control artifacts in this repository.
- No unrelated backlog work or speculative scope reduction.

## Acceptance criteria

- [ ] All specified operational actions are discoverable and documented.
- [ ] JSON output has versioned envelopes and stable exit-code classes.
- [ ] Cancellation, retries, stale revisions, missing daemon, and incompatible versions produce actionable errors.
- [ ] Public contracts and generated schemas remain compatible or are versioned deliberately.
- [ ] `pnpm check` passes from a clean checkout on Node.js 24.
- [ ] The pull request is non-draft, self-contained, and uses `Closes <this issue>`.

## Required tests

- Command/exit-code golden tests.
- Real daemon CLI lifecycle and shell-completion tests on macOS/Linux.
- Regression tests for every defect discovered while implementing this issue.

## Required evidence

- CLI command matrix.
- Machine-readable end-to-end transcript.
- Exact commands and results for local checks.
- GitHub required-check and merge confirmation.

## Autonomous stop conditions

Stop with a typed blocker and evidence if the predecessor is not merged, queue
markers disagree, a product commitment would need to be narrowed, a required
subscription route cannot be proven, credentials could be exposed, or a genuine
technical ambiguity remains after the issue's bounded spike/ADR path.
