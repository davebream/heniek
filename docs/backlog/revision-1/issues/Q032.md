<!-- heniek-queue {"revision":1,"sequence":"Q032","previous":"Q031","milestone":"M3","queueHash":"61fee7f076d3946cfa2a4b82f73f3ec72cf9e8eba6c31cc891277dc7ddd7525a"} -->

# Q032 — Implement one-off graphs, overrides, ad-hoc attachment and pipeline conformance

## Outcome

Users can validate and run one-off graphs, apply controlled overrides, and attach ad-hoc work to an existing pipeline without bypassing canonical state.

## Specification anchors

- §14 Pipeline model
- §8.2 Configuration layers
- §16.1 One canonical state per run

## Dependency

Requires Q031 to be merged and independently confirmed on remote `main`.

## Tier

`T2-capability`

## Constraints

- Overrides are allowlisted, source-traced, and resolved before execution.
- Attachment uses optimistic run revision checks.
- Preserve Product Specification v0.2 commitments and acceptance criteria.
- Keep Windmill, Kombajn, and TAKT out of Heniek runtime dependencies.
- Use strict ESM TypeScript, provider-neutral public contracts, and repository conventions.

## Exclusions

- No mutation of bundled templates.
- No attaching completed work without required artifacts.
- No factory runtime state, credentials, transcripts, or control artifacts in this repository.
- No unrelated backlog work or speculative scope reduction.

## Acceptance criteria

- [ ] One-off graphs pass the same schema and conformance checks as named pipelines.
- [ ] Effective graph and every override are preserved in the run snapshot.
- [ ] Ad-hoc results attach exactly once and release dependants only after validation.
- [ ] Public contracts and generated schemas remain compatible or are versioned deliberately.
- [ ] `pnpm check` passes from a clean checkout on Node.js 24.
- [ ] The pull request is non-draft, self-contained, and uses `Closes <this issue>`.

## Required tests

- Override precedence and forbidden-field tests.
- Concurrent/stale attachment and one-off graph conformance tests.
- Regression tests for every defect discovered while implementing this issue.

## Required evidence

- Effective-graph snapshot.
- Attached ad-hoc stage lifecycle trace.
- Exact commands and results for local checks.
- GitHub required-check and merge confirmation.

## Autonomous stop conditions

Stop with a typed blocker and evidence if the predecessor is not merged, queue
markers disagree, a product commitment would need to be narrowed, a required
subscription route cannot be proven, credentials could be exposed, or a genuine
technical ambiguity remains after the issue's bounded spike/ADR path.
